# Install in the CUDA cloud environment.
torch>=2.4
torchvision>=0.19
opencv-python>=4.8
numpy>=1.24
hydra-core>=1.3
iopath>=0.1.10

# The SAM2 package and its CUDA extension should come from the official repo:
# Clone it into a directory not named "sam2" beside this runner. A directory
# named sam2 shadows the installed Python package when the runner is launched
# from its parent directory.
# git clone https://github.com/facebookresearch/sam2.git sam2_repo
# cd sam2_repo && SAM2_BUILD_CUDA=1 pip install -e .
